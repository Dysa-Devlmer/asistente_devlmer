apiVersion: v1
kind: Service
metadata:
  name: pos-venta-api
  namespace: staging
  labels:
    app: pos-venta-api
    environment: staging
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: pos-venta-api
