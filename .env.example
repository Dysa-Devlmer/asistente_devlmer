# SYSME Docker Environment Configuration

# Application Environment
NODE_ENV=production
APP_NAME=SYSME
APP_VERSION=2.0.0

# Database Configuration
DB_HOST=mysql
DB_PORT=3306
DB_NAME=sysme
DB_USER=sysme_user
DB_PASS=sysme_pass_2024_CHANGE_THIS
DB_ROOT_PASSWORD=sysme_root_2024_CHANGE_THIS

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_min_32_chars_CHANGE_THIS
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Ports Configuration
HTTP_PORT=80
HTTPS_PORT=443
BACKEND_PORT=3001
FRONTEND_PORT=3000
MOBILE_PORT=3002

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:3002,http://localhost:8080

# API Configuration
VITE_API_URL=http://localhost:3001/api/v1

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload Configuration
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,gif,pdf,csv

# Security Configuration
BCRYPT_ROUNDS=12
SESSION_SECRET=your_session_secret_CHANGE_THIS
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100

# Logging Configuration
LOG_LEVEL=info

# SSL Configuration (for production)
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# Backup Configuration
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30
BACKUP_S3_BUCKET=
BACKUP_S3_ACCESS_KEY=
BACKUP_S3_SECRET_KEY=

# Monitoring Configuration
ENABLE_METRICS=true
METRICS_PORT=9090

# Feature Flags
ENABLE_2FA=true
ENABLE_AUDIT_LOG=true
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_SMS_NOTIFICATIONS=false

# Company Configuration
COMPANY_NAME=SYSME TPV
COMPANY_EMAIL=admin@sysme.local
DEFAULT_CURRENCY=CLP
DEFAULT_LANGUAGE=es
DEFAULT_TIMEZONE=America/Santiago

# ============================================
# SII Chile - Facturación Electrónica
# ============================================

# Ambiente SII (CERT = Certificación/Testing, PROD = Producción)
SII_ENVIRONMENT=CERT

# Datos del Emisor (Contribuyente)
SII_RUT_EMISOR=76123456-7
SII_RAZON_SOCIAL=MI EMPRESA LTDA
SII_GIRO=RESTAURANTES Y CAFETERIAS
SII_ACTECO=561000
SII_DIRECCION=AV. PRINCIPAL 1234
SII_COMUNA=SANTIAGO
SII_CIUDAD=SANTIAGO

# Certificado Digital
# Ruta al archivo .pfx o .p12 del certificado
SII_CERT_PATH=/path/to/certificate.pfx
SII_CERT_PASSWORD=your_certificate_password

# CAF (Código de Autorización de Folios)
# Ruta a la carpeta con los archivos CAF
SII_CAF_PATH=/path/to/caf/folder

# Resolución SII (número y fecha de resolución que autoriza DTE)
SII_RESOLUCION_NUMERO=80
SII_RESOLUCION_FECHA=2014-08-22

# Email para envío de DTEs a clientes
SII_EMAIL_SENDER=facturacion@miempresa.cl

# Configuración de reintentos
SII_MAX_RETRIES=3
SII_RETRY_DELAY=5000

# Habilitar/deshabilitar facturación electrónica
ENABLE_FACTURACION_ELECTRONICA=true